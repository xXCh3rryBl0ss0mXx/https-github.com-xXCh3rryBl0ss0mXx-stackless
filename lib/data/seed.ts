import type { Invoice, Lead, Nudge } from "./types";

/** Matches data/leads.csv, invoices.csv, nudge_log.csv (header rows only). */

export const seedLeads: Lead[] = [];
export const seedInvoices: Invoice[] = [];
export const seedNudges: Nudge[] = [];
